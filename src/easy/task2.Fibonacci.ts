/*
 * Задача 1: Реализуйте функцию fib(), возвращающую n-ное число Фибоначчи.
 * Реализовать с использованием мемоизации.
 * Напишите тесты.
 */
export function fib(value: number): number {
	return value;
}

/*
	Примеры:

	fib(0) // 0
	fib(1) // 1
	fib(10) // 55
	fib(20) // 6765
*/
