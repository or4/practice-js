/*
 * Задача c1: Написать функцию bind
 * Напишите тесты.
 */

/*
	Примеры:
	function multiply(value1, value2) {
		return value1 * value2;
	}
	const multiply4 = bind(multiply, null, 4);
	multiply4(11) // 44

	function add(value) {
		this.value += value;
	}
	const testAdd = bind({value: 100});
	testAdd(5); // 105
*/
